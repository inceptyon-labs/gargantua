# Overnight audit — progress log

Session start: 2026-07-24 (evening). Report: `docs/audits/2026-07-25-deep-audit.md`.

## Ambient state noted at start (not touched)

- Uncommitted: `Sources/GargantuaCore/Views/Licensing/UnlockGargantuaSheet.swift` — the 2026-07-14
  unlock-sheet styling fix (link moved below action row, caption font, focusable(false)).
  Never committed. Left as-is; flagged in report.
- Untracked: `docs/audits/` (this audit's own files).

## Phase log

### Phase 0 — Baseline (started)

- Orientation read: AGENTS.md, README.md, PRODUCT.md, CHANGELOG.md (0.4.1–0.4.7), git log -50.
- Note: CHANGELOG 0.4.2 claims pnpm node-shim PATH fix already shipped — Phase 4 pnpm
  investigation must find what still fails beyond that.
- Unlocked `swift build` running in background.

### Phase 0 — Baseline (done)

Both builds clean, 2410 tests pass in 37s, SwiftLint 6 warnings, SwiftFormat clean,
OSV clean, rules snapshot OK, gitleaks clean over 951 commits. 1225 compiler warnings,
all in Tests/, 433 of them Swift-6-fatal.

### Phase 1 — Security (done)

Read myself: privileged helper main.swift (whole), PrivilegedRemovabilityPolicy,
ProcessSpawner, MCPSSERequestRouter, MCPTransportSettings auth, MCPCleanToolHandler
(+Validation), GargantuaMCP/main.swift, LicenseGate, DestructiveActionGate,
LicenseReceiptStorage, TrialClock, LicensePolarConfig, AIExplanationController,
ScheduledScanService (grep), AuditWriter (grep), Sparkle feed check.
Subagent sweep on MCP verified line-by-line before use.

Findings: SEC-01 (dev tools ungated), SEC-02 (no Host validation + no localhost auth),
SEC-03 (MCP clean ungated), SEC-04 (audit after act), SEC-05 (rate limit rename bypass),
SEC-06 (consent notification no-ops unbundled).
Cleared: helper XPC, removability allow-list, spawner, scheduler, AI-cannot-downgrade,
secrets/history, receipt tamper, Sparkle.

### Phase 2/3 — Correctness + complexity (done)

BUG-01 (cwd=/ EROFS, reproduced at CLI, exit 226), BUG-02 (pnpm size hardcoded 0),
CPX-01 (3 dead views verified by reference count).
Complexity metrics from subagent, spot-verified: largest file 535 lines, 1 function
over 100 lines, zero real TODO/FIXME debt, services layer clean of SwiftUI.

Deliberately NOT reported: subagent claimed the mise pnpm shim was broken
("No version is set for shim") — I ran it and it works (11.17.0, exit 0). Killed that
claim; the real cause is the working directory. This is why subagent output gets verified.

### Phase 4 — UX (partial)

Disk Explorer investigation done and verified (UX-01, UX-02).
Developer Tools/pnpm done and reproduced (BUG-01, BUG-02) with a per-tool table
generated by running every preview command from cwd=/ on this machine.
Dead-indicators sweep running.

### Deliberately skipped / not done

- Did NOT drive the app interactively. No screenshots. Every UX finding is code-derived
  plus, for pnpm, a command-line repro. Recorded honestly in report §9.
- Did NOT set up a DNS-rebinding harness for SEC-02; partial curl repro given instead.
- Did NOT run muter (mutation testing) — slow, and the suite is green.

### Phase 4 — UX (done)

Dead-indicator sweep returned 13 gaps; verified the top 6 myself line-by-line before
including. My own first pass had UNDER-reported this area (I grepped only `logger.error`
and concluded it was clean) — the real silences use `logger.warning`, `try?`, and
discarded completion-handler params. Corrected section 4.2 accordingly.

Best finding of the night: DashboardView.swift:263-266 records a triage scan error that
DashboardTriageEvidenceView never reads, so a FAILED scan renders a green checkmark
"No triage groups found". Verified: grep for `errors` in that view file returns nothing.

New findings added: UX-05 (dashboard lies), UX-06 (duplicate finder failures hidden),
BUG-03 (model delete unverified), UX-07 (batch of 9 log-only failures).

### Phase 5/6/7 — cross-cutting, features, self-review (done)

Deps current (Sparkle 2.9.2 CVE-patched, Yams 5.4.0, mlx-swift-lm 3.31.3); vendored bins
pinned by SHA256; release pipeline verified (build.sh:24 exports GARGANTUA_LICENSING=1,
sign/notarize/EdDSA appcast all present).

Three features, each earned from something seen: undo-from-audit-trail (the trashPath is
already computed in CleanupItemResult.trashURL and dropped when AuditFile is built),
agent plan-approval handshake (dry_run already returns a plan shaped like a real run),
provenance search (explain already reverse-matches arbitrary paths against the rule set).

Self-review: ran an automated citation checker over the report — 94 unique file:line
citations, all resolve. The only non-resolving paths are test files proposed in
acceptance criteria; corrected one that pointed at a since-split test file.

FINAL: 21 findings — 5 High, 10 Medium, 6 Low.
